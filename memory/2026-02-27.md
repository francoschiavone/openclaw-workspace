# Memory — 2026-02-27

## Lumber HRIS Project Status (2:38 AM)

### Completed
- Full-stack HRIS app for "Summit Construction Group" at `/home/node/.openclaw/workspace/lumber-hris/`
- Backend: FastAPI + SQLAlchemy + SQLite, 22 tables, 8 routers (auth, employees, departments, org_chart, performance, lms, analytics, dashboard)
- Frontend: Vite + React 19 + TypeScript + Tailwind CDN, 8 pages (Dashboard, Employees, EmployeeProfile, OrgChart, Performance, LMS, Analytics, Login)
- Seed data: 374 employees, 835 certs, 932 training assignments, 350 reviews, 35 courses, 53 goals, 12 incidents, 25 commendations, 3 PIPs, 8 projects, 24 users
- Frontend build: 2664 modules → `frontend/dist/`, served by FastAPI catch-all
- Start: `cd lumber-hris && bash start.sh` or `DATABASE_URL="sqlite:///./lumber_hris.db" PYTHONPATH=. uvicorn backend.main:app --port 8000`
- Demo logins: admin@lumber.com/LumberAdmin2026!, hr@lumber.com/LumberHR2026!, pm@lumber.com/LumberPM2026!, foreman@lumber.com/LumberForeman2026!, worker@lumber.com/LumberWorker2026!

### Audit vs PLAN.md — Known Gaps
- Missing `/api/auth/refresh` endpoint
- Employee count 374 vs target 377+
- Missing: CSV export, create/edit employee forms, status change modal
- Org chart: no project/crew toggle, no node-click navigation, no breadcrumbs/fullscreen/zoom
- Performance: review creation/update may not work, no sign-off workflow
- LMS: missing worker/manager views, rules management UI, gap analysis endpoint
- Dashboard: missing date range/department filters, activity feed
- Phase 8 (Docker/Polish): not started — no docker-compose, README, error boundaries, empty states

### Blocked
- `httpx` not installed in venv (needed for TestClient approach)
- Background uvicorn processes timeout in exec environment
- Need to install: `/home/node/.openclaw/workspace/.venv/bin/pip install httpx`

### Next Steps
1. Install httpx, run comprehensive test script (33 endpoints)
2. Fix demo-impactful issues first
3. Create video demo
4. Push to GitHub

## Environment Notes
- npm global prefix: `/home/node/.openclaw/workspace/.npm-global`
- Python venv: `/home/node/.openclaw/workspace/.venv/`
- `/tmp` and `/home/node/.openclaw` are noexec mounts
- Root filesystem is READ-ONLY
- bcrypt==4.2.1 required (5.x incompatible with passlib)
- PLAN.md: 81KB, 1459 lines, 9 phases at `lumber-hris/PLAN.md`

## LinkedIn
- 3 post topics proposed to Franco, awaiting selection
- Ideas Backlog at `obsidian/03-Career/LinkedIn/Ideas-Backlog.md`

## Cron Fixes Done
- GitHub backup cron (job c5b5a57a) — fixed paths
- iCloud monitor cron (job 4de77bde) — fixed timeout
