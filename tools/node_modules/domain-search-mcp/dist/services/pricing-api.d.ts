/**
 * Pricing API client (backend).
 *
 * This MCP does NOT ship registrar secrets. Pricing is retrieved from a
 * centralized backend (Vercel) that owns the registrar API keys.
 */
import type { PricingStatus } from '../types.js';
export type PricingQuote = {
    registrar: string;
    available: boolean | null;
    premium: boolean | null;
    price_first_year: number | null;
    price_renewal: number | null;
    price_transfer: number | null;
    currency: string | null;
    quote_status: PricingStatus;
    message?: string;
};
export type PricingQuoteResponse = {
    domain: string;
    quotes: PricingQuote[];
    best_first_year: {
        registrar: string;
        price: number;
        currency: string | null;
    } | null;
    best_renewal: {
        registrar: string;
        price: number;
        currency: string | null;
    } | null;
    best_transfer: {
        registrar: string;
        price: number;
        currency: string | null;
    } | null;
    quote_status: PricingStatus;
    message?: string;
};
export type PricingCompareEntry = {
    registrar: string;
    domain: string;
    price_first_year: number | null;
    price_renewal: number | null;
    price_transfer: number | null;
    currency: string | null;
    source: 'catalog' | 'live';
    quote_status: PricingStatus;
    available?: boolean | null;
    premium?: boolean | null;
    message?: string;
};
export type PricingCompareResponse = {
    domain: string;
    comparisons: PricingCompareEntry[];
    best_first_year: {
        registrar: string;
        price: number;
        currency: string | null;
    } | null;
    best_renewal: {
        registrar: string;
        price: number;
        currency: string | null;
    } | null;
};
export declare function fetchPricingQuote(fqdn: string): Promise<PricingQuoteResponse | null>;
export declare function fetchPricingCompare(domain: string, tld: string, registrars?: string[]): Promise<PricingCompareResponse | null>;
//# sourceMappingURL=pricing-api.d.ts.map